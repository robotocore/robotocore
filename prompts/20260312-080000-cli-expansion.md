---
session: "cli-expansion-ac4fa3f9"
timestamp: "2026-03-12T08:00:00Z"
model: claude-opus-4-6
---

## Human

Comprehensively expand the robotocore CLI at `src/robotocore/cli.py`. The current CLI only covers container lifecycle and basic state. There are 47 management API endpoints that should be accessible. Add 11 new command groups: services, config, chaos, resources, audit, usage, pods, ses, iam, diagnose, and version. Add table formatting, --format flag, --wait on start, --tail on logs.

## Assistant

## Key decisions

**Reusable `_api_cmd` helper**: Extracted a common pattern from the existing commands into `_api_cmd()` that wraps `_api_request` with standard error handling. All new commands use this, reducing boilerplate. Existing commands were left unchanged to avoid breaking their established test mocks.

**`_print_table` helper**: Simple column-aligned formatting using `str.ljust()`. No external dependencies. Handles empty rows with "(no data)" message. Used by all list-like commands when `--format table` is active.

**Format flag placement**: Added `--format` as a per-subparser flag rather than global, because not all commands benefit from it (e.g., `start`, `stop`, `logs`). Default is `table` for list-like commands, `json` for single-object responses.

**Subcommand dispatch pattern**: Kept the existing pattern of dispatch dicts. New command groups with subcommands (config, chaos, pods, ses, iam, usage) use a similar pattern to `state` -- a top-level `cmd_X` that reads `X_command` from args and dispatches to internal `_cmd_X_Y` functions.

**`--wait` on start**: Reuses `cmd_wait` by constructing a minimal Namespace, avoiding code duplication.

**Status command enhancement**: When container shows "Up" in docker status, also hits the health endpoint to show version and service count. Silently catches errors (container might be starting up).

**Test strategy**: 131 tests total (up from 50). Each new subcommand has at least one parsing test and one dispatch test. Table formatter has dedicated tests for basic output, empty rows, and column alignment. Tests for commands using `_api_cmd` include `data=None` in assertions since the helper always passes all kwargs through.
