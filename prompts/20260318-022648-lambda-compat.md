---
session: "a6f6be35"
timestamp: "2026-03-18T02:26:48Z"
model: claude-sonnet-4-6
---

## Human

Expand compat tests for the lambda service. The server is running on port 4566.
Goal: add tests for untested Lambda operation chunks in chunks/lambda.json.
Write tests in tests/compatibility/test_lambda_compat.py.

Rules: run each test against live server before keeping, never catch ParamValidationError,
every test must assert something, commit every 3-4 chunks, push to lambda-compat-expansion.

## Assistant

## Key decisions

**Chunk file mapping**: chunks/lambda.json for the lambda service is generated by
chunk_service.py which groups operations by resource noun. 13 of the 20 untested
operations are not implemented (GetCapacityProvider, ListCapacityProviders, all
DurableExecution variants, etc.) — these were confirmed via probe and skipped entirely
per the "fix-then-test" rule.

**Implemented and testable**: The probe showed CreateCodeSigningConfig, GetCodeSigningConfig,
DeleteCodeSigningConfig, ListCodeSigningConfigs, and ListFunctionsByCodeSigningConfig are all
working. These 5 operations across 2 chunks were implemented and verified.

**ListFunctionsByCodeSigningConfig response shape**: The response uses `FunctionArns` (not
`Functions`) — discovered by inspecting the actual server response. Wrote the assertion
to match the actual response key.

**UpdateCodeSigningConfig**: Probe showed this returns InvalidRequest (not implemented),
so no test was written. Do not test unimplemented operations.

**7 new tests added** across 2 new classes (TestLambdaCodeSigningConfigCRUD,
TestLambdaListFunctionsByCodeSigningConfig). All 7 pass against the live server.
Full suite went from 133 to 140 passing tests. Quality gate: 0% no-server-contact rate.
