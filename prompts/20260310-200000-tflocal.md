---
session: "ac180393"
timestamp: "2026-03-10T20:00:00Z"
model: claude-opus-4-6
---

## Human

Implement `tflocal` — a Terraform CLI wrapper that auto-configures Terraform to use robotocore endpoints. Requirements: bash and Python versions, auto-generate provider override files with all service endpoints, set AWS credentials as defaults, support ROBOTOCORE_HOST/PORT/ENDPOINT env vars, TF_COMPAT_MODE, cleanup override files after terraform exits. Write tests first.

## Assistant

## Key decisions

**Architecture**: Single Python module (`cli/tflocal/tflocal.py`) with testable functions, plus a thin bash wrapper that delegates to it. The Python module is the source of truth for all logic.

**Override file format**: Used `.tf.json` (JSON format) instead of HCL for the override file. JSON is easier to generate programmatically and Terraform natively supports it.

**Service list**: Included 240+ Terraform AWS provider service endpoint names. This covers all services the Terraform AWS provider supports, mapping each to the single robotocore endpoint.

**Command detection**: Only generates override files for infrastructure-modifying commands (init, plan, apply, destroy, import, refresh, etc.). Commands like `fmt`, `validate`, `version` don't need provider configuration.

**Credential handling**: Default credentials (`test`/`test`/`us-east-1`) are only set when the user hasn't already set them. This prevents overriding real credentials in hybrid setups.

**Test strategy**: 48 tests total — 37 unit tests covering each function in isolation, 11 integration-style tests covering the full lifecycle with mocked subprocess. All tests pass without requiring terraform installed.
