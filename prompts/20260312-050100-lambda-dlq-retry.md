---
session: "find-every-problem"
timestamp: "2026-03-12T05:01:00Z"
model: claude-opus-4-6
sequence: 3
---

## Human

Find every problem with robotocore and fix it.

## Assistant

## Key decisions

**Bug**: Failed async Lambda invocations went straight to DLQ without retrying.
AWS retries up to MaximumRetryAttempts (default 2) before sending to DLQ.
The DLQ record claimed "RetriesExhausted" but no retries occurred.

**Fix**: Added `_execute_function()` helper that extracts Lambda execution logic
for retry use. Modified `_dispatch_async_result()` to read MaximumRetryAttempts
from event invoke config and retry before DLQ dispatch. Uses recursion with
`_retry_attempt` counter.
