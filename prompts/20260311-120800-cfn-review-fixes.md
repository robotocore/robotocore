---
session: ci-fix-wave1
timestamp: "2026-03-11T12:08:00Z"
model: claude-opus-4-6
---

# Fix CI for CloudFormation review tests PR

## Human

Fix CI failures on Wave 1 PR #89: add prompt log and fix unit:services test failures.

## Assistant

## Key decisions

**Empty Resources validation**: The CFN provider now validates that templates have a non-empty `Resources` section (matching AWS behavior). Three test templates used `"Resources": {}` which triggers this validation. Fixed by adding a minimal `AWS::CloudFormation::WaitConditionHandle` resource to each template.

**GetAtt unknown resource**: `test_get_att_unknown_resource` expected empty string but the engine now raises `ValueError` with "undefined resource" message (matching AWS). Updated test to `pytest.raises(ValueError)`.

**Select out of bounds**: `test_select_out_of_bounds` expected empty string but the engine now raises `ValueError` (matching AWS). Updated test to expect the error.

**ImportValue not found**: `test_import_value_not_found` expected the import name echoed back, but the engine now raises `ValueError` with "No export named" message (matching AWS). Updated test and added `pytest` import.
