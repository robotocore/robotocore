---
session: "tier1-e2e-tests"
timestamp: "2026-03-10T00:30:00Z"
model: claude-opus-4-6
---

## Human

Write the long tail of missing test coverage for the 4 Tier 1 enterprise features.
Anticipate any way a user might use these APIs and test that the highest-level logic
is sound and complete.

## Assistant

## Key decisions

Launched 4 parallel agents, one per feature, each given exhaustive lists of
real-world usage scenarios to test. Focus on end-to-end flows, edge cases, error
handling, and concurrency — things unit tests miss.

**Multi-account (47 tests):** Cross-account resource visibility (SQS, SNS, Kinesis,
DynamoDB), cross-account operations that should fail, account ID extraction edge
cases (malformed auth, leading zeros, presigned URLs), state persistence roundtrip,
resource browser scoping, concurrent multi-account access, region+account 2D
isolation, ARN correctness.

**Database engines (148 tests):** Real SQL patterns (JOINs, CTEs, window functions,
JSON functions, UPSERT), transaction edge cases (nested, concurrent, error recovery),
constraint violations, response format accuracy vs AWS spec. Redis patterns (session
store, rate limiting, queue, hash profiles), data integrity (unicode, null bytes,
binary), TTL deep tests (lazy expiry, PERSIST, RENAME+TTL), unsupported command
errors.

**Lambda hot reload (60 tests):** Developer workflow simulation (repeated invoke,
update, re-invoke), mount directory lifecycle, cache memory management (LRU at 60
entries), code extraction edge cases (nested dirs, packages, corrupt zips), layer
interaction, sys.modules cleanup (stdlib preserved, local cleared), concurrency
(10 threads), handler errors, environment variables, LambdaContext fields.

**EKS mock k8s (75 tests):** Full cluster lifecycle over real HTTP, multi-cluster
isolation, k8s API edge cases (duplicate 409, missing metadata 400, extra fields
preserved), namespace cascade delete, API discovery structure for kubectl, response
format accuracy (UUID4 UIDs, ISO timestamps, monotonic resourceVersion), server
lifecycle edge cases, memory cleanup, deployment replicas, kubeconfig live
integration.

Using PR flow since pushes to main are now blocked.
