---
session: "sqs-cloudwatch-metrics"
timestamp: "2026-03-10T12:00:00Z"
model: claude-opus-4-6
---

## Human

Implement SQS CloudWatch metrics reporting for robotocore. Automatically publish
queue metrics (visible/inflight/delayed counts, message age, send/receive/delete
counters, empty receives, message size) to CloudWatch namespace AWS/SQS with
QueueName dimension. Background thread publishes every 60s (configurable via
SQS_METRICS_INTERVAL). Use Moto's CloudWatch backend directly. Thread-safe
counters with reset each cycle. Configurable via SQS_CLOUDWATCH_METRICS env var.

## Assistant

Created `src/robotocore/services/sqs/metrics.py` with:
- Thread-safe per-queue counters (sent, received, deleted, empty_receives, sent_bytes)
- `compute_queue_metrics()` for gauge metrics from queue state
- `publish_metrics()` writes to Moto's CloudWatch backend directly
- `SqsMetricsPublisher` background daemon thread (configurable interval, env-gated)
- Counter hooks wired into SQS provider (SendMessage, ReceiveMessage, DeleteMessage,
  SendMessageBatch)
- Background publisher started alongside other engines in `_start_background_engines()`

Tests: 20 unit tests + 7 integration tests, all passing.
