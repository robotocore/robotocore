---
session: "ecs-unit-tests"
timestamp: "2026-03-12T05:21:00Z"
model: claude-opus-4-6
---

## Human

Write comprehensive unit tests for the ECS native provider at `src/robotocore/services/ecs/provider.py`. Create the test file at `tests/unit/services/ecs/test_ecs_provider.py`. Test each action function directly by importing them and calling with mock data. Cover edge cases: missing params, invalid names, task state transitions. Test the task lifecycle: register task def, create service, run task, stop task.

## Assistant

## Key decisions

**Direct function calls, not HTTP layer**: The existing `tests/unit/services/test_ecs_provider.py` already tests via `handle_ecs_request` (async HTTP). The new file calls the inner action functions (`_create_cluster`, `_run_task`, etc.) directly with `EcsStore` instances, testing pure business logic without async/HTTP overhead.

**162 tests covering all 31 action functions**: Every function in `_ACTION_MAP` gets at least one test. Edge cases include: missing cluster errors, duplicate service names, ARN vs name resolution, tag cascade on delete, task definition revision atomicity, inactive status filtering, and multi-region/account isolation.

**Test categories**:
- Store initialization and isolation (region, account)
- Helper functions (_resolve_cluster_name, _require_cluster, _resolve_task_definition, _find_resource_by_arn)
- Cluster CRUD (create, describe, list, delete with cascade)
- Task definitions (register, describe, list, list families, deregister, delete batch)
- Services (create, describe, list, update, delete with tag cleanup)
- Tasks (run, describe, list with status filter, stop with reason)
- Container instances (register, deregister, describe, update state, list)
- Task sets (create, describe, update scale, delete)
- Attributes (put, delete, list with filters)
- Tagging (tag, untag, list, inline sync)
- Capacity providers
- Full lifecycle integration test
- Multi-region and multi-account isolation
