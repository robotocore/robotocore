---
session: "agent-aefc806f"
timestamp: "2026-03-11T05:49:07Z"
model: claude-opus-4-6
---

## Human

Implement `pulumilocal` — a Pulumi CLI wrapper that auto-configures Pulumi to use robotocore endpoints. Create both bash and Python versions in `cli/pulumilocal/`. Write tests first. The wrapper should set AWS_ENDPOINT_URL, credentials, PULUMI_CONFIG_PASSPHRASE, PULUMI_BACKEND_URL, and support ROBOTOCORE_HOST/PORT/ENDPOINT env vars. Don't override user-set values.

## Assistant

## Key decisions

**Test-first approach**: Wrote 34 tests across two files before implementing. Tests cover default endpoint, custom host/port, endpoint override, credential defaults, credential preservation, passphrase, backend URL, argument passthrough, pulumi config generation, bash script existence/executability, and error handling for missing pulumi.

**Python module as importable library**: Structured `pulumilocal.py` with pure functions (`build_env`, `build_command`, `build_pulumi_config`, `check_pulumi_installed`) so tests can import and test each function independently without subprocess overhead.

**Endpoint resolution precedence**: ROBOTOCORE_ENDPOINT > ROBOTOCORE_HOST+ROBOTOCORE_PORT > defaults (localhost:4566). AWS_ENDPOINT_URL is always set (not user-overridable) since that's the whole point of the wrapper.

**Credentials are user-overridable**: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_DEFAULT_REGION, PULUMI_CONFIG_PASSPHRASE, and PULUMI_BACKEND_URL all use "set if absent" semantics so users can override them.

**Pulumi provider config**: `build_pulumi_config()` generates the aws:endpoints JSON array format that Pulumi's AWS provider expects, mapping 60+ service names to the robotocore endpoint.

**Bash script uses exec**: The bash version uses `exec pulumi "$@"` to replace the shell process, avoiding an unnecessary parent process.
